projects:
  app:
    schemaPath: src/graphql/schema/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000/graphql
  prisma:
    schemaPath: src/graphql/generated/prisma.graphql
    extensions:
      prisma: prisma/prisma.yml